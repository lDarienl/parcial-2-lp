[project]
name = "punto5-recursive-descent"
version = "0.1.0"
description = "Parser descendente recursivo con match: asignaciones y condicionales"
requires-python = ">=3.10"

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
